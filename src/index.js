import React from 'react'
import ReactDOM from 'react-dom';
import BasicExample from './BasicExample'

const $reactRoot = document.querySelector('#root');
ReactDOM.render(<BasicExample />, $reactRoot);
